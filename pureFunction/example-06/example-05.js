

const numbers = [1,2,3]

numbers.reduce((previos, current) => previos  + current, 0)

const sum = (x, y) => x + y

numbers.reduce(sum, 0)


// example live code
// return address
// https://jsonplaceholder.typicode.com/users
