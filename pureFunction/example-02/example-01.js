

// function calculate(fn, x, y)  {
//   return fn(x, y)
// }

const calculate = (fn, x, y) => fn(x, y)

// function sum(x, y) {
//   return x + y
// }

const sum = (x, y) => x + y

const mult = (x, y) => x * y
