

const plus = (value) => value + 1;

[1,2,3,4].map(plus);
[1,2,3,4].filter(plus);
[1,2,3,4].forEach(plus);


const recebe =  new Array(10)

for (let i = 0; i < recebe.length ; i++) {
  console.log('i', i)
}

